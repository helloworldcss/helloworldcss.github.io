(() => {
  // grab the .navArrow from the html
  let navArrow = document.querySelectorAll('.navArrow');
  // grab the navArrow styles for less typing later on
  let navArrowS = navArrow.styles;
  // grab the


})();
